#include <algorithm>
#include <string>
#include <vector>

using namespace std;

int solution(vector<int> people, int limit) {
  int answer = 0;
  int curWeight = 0;

  sort(people.begin(), people.end(), greater<int>());

  return answer;
}