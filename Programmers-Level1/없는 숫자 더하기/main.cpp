#include "solution.h"

int main() {
  vector<int> numbers;

  cout << solution(numbers = {1, 2, 3, 4, 6, 7, 8, 0}) << endl;

  cout << solution(numbers = {5, 8, 4, 0, 6, 7, 9}) << endl;

  return 0;
}