#include "solution.h"

int main() {
  vector<int> nums;

  cout << solution(nums = {3, 1, 2, 3}) << endl;

  cout << solution(nums = {3, 3, 3, 2, 2, 4}) << endl;

  cout << solution(nums = {3, 3, 3, 2, 2, 2}) << endl;

  return 0;
}